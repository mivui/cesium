/**
 * 在 {@link StripeMaterialProperty} 中定义了条带的方向。
 *
 * @enum {number}
 */
const StripeOrientation = {
  /**
   * 水平方向。
   * @type {number}
   */
  HORIZONTAL: 0,

  /**
   * 垂直方向。
   * @type {number}
   */
  VERTICAL: 1,
};
export default Object.freeze(StripeOrientation);
